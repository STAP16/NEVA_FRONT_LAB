DATABASE_URL="postgresql://postgres:postgres@localhost:5433/neva_lab?schema=public"
JWT_SECRET="<generate-with: node -e \"console.log(require('crypto').randomBytes(48).toString('hex'))\">"
ADMIN_USERNAME="admin"
# Optional: provide only for first server start. Do not commit real values.
# ADMIN_INITIAL_PASSWORD="<set-temporarily-or-leave-empty>"
PORT=3001
